import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "no.olefroiland.amerikaneren",
  appName: "Amerikaneren",
  webDir: "dist",
};

export default config;
